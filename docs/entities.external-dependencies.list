#!/bin/bash
#! shellcheck disable=SC2034

# These packages are required by entities.bash
declare -a _ent_ESSENTIAL_PACKAGES=(
	binutils
	errno
	gzip
	locate
	moreutils
	shellcheck
	tree
	unzip
	util-linux
	wget
	zip
)

# Recommended for entities.bash (YMMV)
declare -a _ent_RECOMMENDED_PACKAGES=(
	bzip2
	curl
	git
	hexedit
	imagemagick
	joe
	lynx
	w3m
)

# If php/mysql is required
declare -a _ent_PHP_MYSQL_CLIENT_PACKAGES=(
	mysql-client
	php-cli
	php-mysql
	php-pear
	php-xml
	php-zip
)

#fin
